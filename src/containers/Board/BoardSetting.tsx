import React from 'react';
import { Text } from 'react-native-paper';
import { View } from 'react-native';

type Props = {};

const BoardSetting = (props: Props) => {
  return (
    <View>
      <Text>BoardSetting</Text>
    </View>
  );
};

export default BoardSetting;
